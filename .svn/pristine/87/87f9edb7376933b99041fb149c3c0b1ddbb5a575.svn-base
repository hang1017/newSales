export interface CouponsPopViewProps {
  enableData: CouponsPopItemProps[]; // 可用优惠券
  disableData: CouponsPopItemProps[]; // 不可用优惠券
  onSubmit: (list: CouponsPopItemProps[]) => void;
}

export interface CouponsPopItemProps {
  id: string;
  amount: string;
  name: string;
  phoneNumber: string;
  expiryDate: string;
  select: boolean;
}
